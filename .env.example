# ─── API ────────────────────────────────────────────────────────
# Dev: НЕ задавать — фронт сам берёт origin + /api
#      localhost:3047/api → Vite proxy → http://teleback
#
# Prod: задать явно, т.к. бэк на субдомене api.teftele.com
VITE_API_URL=https://api.teftele.com/api
